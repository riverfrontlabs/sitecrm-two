import { describe, it } from 'vitest';

/** HomePage is now a redirect wrapper — no meaningful UI to test. */
describe('HomePage', () => {
  it('is a stub redirect — see DashboardPage.tsx for the actual dashboard', () => {
    // intentionally empty
  });
});
