# Dependencies
node_modules/

# Build output
dist/
build/

# Test artifacts
coverage/

# Environment files (commit .env.example instead)
.env
.env.*
!.env.example

# Terraform: state (contains secrets!), caches, and local variable values
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
tfplan
*.tfplan
# (note: .terraform.lock.hcl is intentionally committed — do not ignore it)

# Local AI-tooling state
.claude/settings.local.json
.qodo/

# Editor & OS noise
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json
